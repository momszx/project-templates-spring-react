import React from "react";
function ReceptionOnlyButton(props){
        return(
            <div>
                <button className={"btn btn-primary"}>Guest check in</button>
                <button className={"btn btn-primary"}>Guest check out</button>
            </div>
        )
}

export default ReceptionOnlyButton